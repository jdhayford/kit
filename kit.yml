commands:
  dinspect:
    command:
      docker ps @test
    description:
      Gets running docker containers
    arguments:
      test:
        required: true
        prompt: Test select arg
        type: select
        optionCommand: docker ps
